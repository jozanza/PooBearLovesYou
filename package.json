{
  "name": "poobear-loves-you",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "start": "parcel index.html",
    "prestart": "npm run clean && npm run copy-assets",
    "clean": "rm -rf dist",
    "copy-assets": "mkdir -p dist && cp -r assets dist/assets",
    "build": "parcel build index.html"
  },
  "dependencies": {
    "@vsmode/starship": "^1.0.1"
  },
  "devDependencies": {
    "@babel/core": "7.2.0",
    "parcel-bundler": "^1.6.1",
    "typescript": "^3.7.5"
  },
  "keywords": []
}
